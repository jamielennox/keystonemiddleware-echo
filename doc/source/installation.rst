============
Installation
============

At the command line::

    $ pip install keystonemiddleware-echo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv keystonemiddleware-echo
    $ pip install keystonemiddleware-echo
